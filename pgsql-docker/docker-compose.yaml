services:
  pgsql:
    image: postgres:17
    container_name: pgsql_${STUDENT_ID}
    tty: true
    stdin_open: true
    environment:
      PGPASSWORD: ${STUDENT_PASSWORD}
    entrypoint: ["bash"]
    volumes:
      - ./data:/data